import { EditorState, SelectionState } from 'draft-js';
import { WordAtResult } from './getWordAt';
export default function getSearchText(editorState: EditorState, selection: SelectionState): WordAtResult;
