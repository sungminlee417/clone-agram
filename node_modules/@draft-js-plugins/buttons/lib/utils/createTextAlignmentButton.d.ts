import { ReactNode } from 'react';
import { DraftJsStyleButtonType } from '..';
interface CreateTextAlignmentButtonProp {
    alignment: string;
    children: ReactNode;
}
export default function createTextAlignmentButton({ alignment, children, }: CreateTextAlignmentButtonProp): DraftJsStyleButtonType;
export {};
